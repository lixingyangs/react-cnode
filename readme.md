~~~
yarn config set registry https://registry.npm.taobao.org
~~~

~~~
Objects are not valid as a React child (found: object with keys {__esModule, default}). If you meant to render a collection of children, use an array instead.
~~~

~~~
Target container is not a DOM element  // hydrate
~~~